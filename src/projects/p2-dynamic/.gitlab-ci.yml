stages:
  - build
  - deploy

build:
  stage: build
  artifacts:
    paths:
      - vendor/
  script:
    - make build

deploy:
  stage: deploy
  dependencies:
    - build
  script:
    - mv .env-prod .env
    - make deploy
